// Re-export types from Next.js to avoid import conflicts
import type { Metadata as NextMetadata } from "next"

// Re-export the Metadata type
export type Metadata = NextMetadata

// You can add other re-exports here if needed

